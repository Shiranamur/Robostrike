<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">WebRobotStrike</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler"/>

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="weather">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Weather
            </NavLink>
        </div>

        <div class="nav-item px-3 dropdown">
            <NavLink class="nav-link dropdown-toggle" href="map" id="mapDropdown" role="button">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Map
            </NavLink>
            <ul class="dropdown-menu" aria-labelledby="mapDropdown">
                <li>
                    <NavLink class="dropdown-item" href="map/editor">
                        <span class="bi bi-pencil-fill" aria-hidden="true"></span> Éditeur de carte
                    </NavLink>
                </li>
            </ul>
        </div>
    </nav>
</div>

<style>
    /* Afficher le dropdown au survol */
    .dropdown:hover .dropdown-menu {
        display: block;
    }

    /* Style pour correspondre au thème sombre */
    .dropdown-menu {
        background-color: var(--bs-dark);
        border: 1px solid rgba(255,255,255,0.1);
    }

    .dropdown-item {
        color: rgba(255,255,255,0.7);
    }

    .dropdown-item:hover {
        color: white;
        background-color: rgba(255,255,255,0.1);
    }

    .dropdown-item.active,
    .dropdown-item:active {
        color: #fff;
        background-color: var(--bs-primary);
    }

    /* Supprime la flèche du dropdown par défaut */
    .dropdown-toggle::after {
        display: none;
    }
</style>